# Peer dependency settings
strictPeerDependencies: true # fail install if peer dependencies are missing or incompatible

# Security settings
minimumReleaseAge: 1440 # minimum number of minutes that must pass after a version is published before pnpm will install it
minimumReleaseAgeExclude:

onlyBuiltDependencies:
    - '@evilmartians/lefthook'
    - sharp

# Override specific packages to resolve peer dependency issues or vulnerabilities
overrides:
