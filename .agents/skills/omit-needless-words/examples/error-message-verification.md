# Example: error message and verification

## Original

If you enter an incorrect API key, you might potentially see authentication issues, so we recommend
that you double-check the value in the configuration field below.

## Light

If the API key is incorrect, you may see authentication issues. Double-check the value in the
configuration field.

## Balanced

An incorrect API key causes authentication errors. Verify the key in the configuration field.

## Strong

Wrong API key causes authentication errors. Verify the configuration value.

## Notes

- Replaces hedging with direct cause and effect.
- Keeps the recovery action.
- Keeps the field context unless strong intensity can safely compress it.
